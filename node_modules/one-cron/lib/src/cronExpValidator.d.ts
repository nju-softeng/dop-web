/**
 * Validates a cron expression.
 *
 * @param cronExpression The expression to validate
 * @return True is expression is valid
 */
export declare function cronValidate(cronExpression: string): boolean;
